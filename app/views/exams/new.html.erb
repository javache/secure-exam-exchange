<%= form_for @exam do |f| %>

  <%= f.file_field :data %> <br \>

  <% @users.each do |u| %>
    <%= label_tag "user[#{u.id}]", u.name %>
    <%= check_box_tag "user[#{u.id}]", true, true %><br \>
  <% end %>

  <%= f.submit %>
<% end %>
